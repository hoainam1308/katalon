name: Katalon CI/CD

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  katalon-test:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout code từ GitHub
        uses: actions/checkout@v3

      - name: 🔽 Download & Cài đặt Katalon Studio
        run: |
          wget https://download.katalon.com/9.7.5/Katalon_Studio_Linux_64-9.7.5.tar.gz
          tar -xvzf Katalon_Studio_Linux_64-9.7.5.tar.gz -C /opt/
          ls -l /opt/  # Kiểm tra file đã giải nén

      - name: 🛠 Kiểm tra file sau khi tải
        run: |
          wget https://download.katalon.com/9.7.5/Katalon_Studio_Linux_64-9.7.5.tar.gz
          tar -xvzf Katalon_Studio_Linux_64-9.7.5.tar.gz -C /opt/
          echo "🟢 Kiểm tra nội dung thư mục /opt/"
          ls -l /opt/
          echo "🟢 Kiểm tra tất cả các file trong /opt/"
          find /opt/ -type f


      - name: 🛠 Cấp quyền thực thi cho Katalon
        run: |
          chmod +x /opt/Katalon_Studio_Linux_64-9.7.5/katalonc

      - name: 🚀 Chạy Katalon Test Suite
        run: |
          /opt/Katalon_Studio_Linux_64-9.7.5/katalonc -noSplash -runMode=console \
            -projectPath="$(pwd)/TestLogin" \
            -retry=0 -testSuitePath="Test Suites/Test_Suite_Login" \
            -executionProfile="default" -browserType="Chrome"

      - name: 📤 Upload báo cáo kiểm thử
        uses: actions/upload-artifact@v4
        with:
          name: katalon-test-report
          path: "Reports"
